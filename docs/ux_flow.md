# 1) Пользовательский сценарий (личный чат)

## 1.1. Первое касание (`/start`)

* **Ленивая инициализация** профиля: создать пользователя с дефолтными `prefs` и пустой `session`.
* Показать «Главное меню»: «Играть», «Рейтинг» (заглушка), «Настройки», «Темы», «Помощь».

## 1.2. Настройки

* Поля: **qcount** (3/5/10), **diff** (e/m/h), **sol** (imm/end).
* Тап по значению мгновенно применяет настройку (визуальная метка активного значения).
* Возврат в меню одной кнопкой.

## 1.3. Темы

* Сетка 2×N из `topics.json` с пагинацией.
* Тап по теме → попытка стартовать серию на `prefs.qcount` и `prefs.diff`.
* Если вопросов не хватает: ненавязчивое уведомление «В этой теме пока мало вопросов. Выберите другую тему или снизьте сложность» и остаться на экране тем.

## 1.4. Режимы игры

* **Серия**: N вопросов по теме/сложности.
* **Случайный**: N вопросов по всему банку при выбранной сложности.
* **Дневное**: 1 вопрос, те же правила рендера и ответа.

## 1.5. Экран вопроса (общий каркас)

* Заголовок: «Тема · i/N · сложность».
* Текст: `statement` + краткое `question`.
  Для таблиц/матриц — моноширинный формат. LaTeX не использовать.
* Нижняя панель:

  * «🧠 Решение» (немедленно или «в конце», согласно prefs);
  * «Дальше →» (активна только после принятого ответа) / «Завершить серию» на последнем;
  * «🏠 Меню».
* Допкнопки зависят от типа ответа:

  * `numeric`/`text`: «Ввести ответ» / «Отмена ввода»
  * `multi_choice`: «Принять выбор» / «Очистить»

## 1.6. Типы заданий (UX-поведение)

* **single_choice**
  Варианты списком кнопок (до 8 на экран). Тап по варианту фиксирует ответ, показывает краткий фидбек, активирует «Дальше →».
  При длинных вариантах — 2 варианта в ряд, перенос строк допустим.
* **multi_choice**
  Варианты с чек-индикатором: «☐»/«☑». Выбор сохраняется локально; после «Принять выбор» — проверка и фидбек. При >8 вариантах — страничная навигация по вариантам, выделение сохраняется.
* **numeric**
  «Ввести ответ» включает режим ожидания текста. Подсказка: «Отправьте число одним сообщением (запятая = точка). Нажмите “Отмена ввода”, чтобы вернуться».
  Нормализация: убрать пробелы, заменить запятую на точку, запрет NaN/∞. Сравнение целочисленное (по банку).
* **text**
  «Ввести ответ» → ожидание текста. Нормализация: `lower`, обрезать пробелы по краям, схлопнуть множественные пробелы. Пунктуацию по краям — игнорировать. Синонимы сейчас **не поддерживать** (строгое сравнение после нормализации).

## 1.7. Решение/объяснение

* Режим **imm**: «🧠 Решение» разворачивает объяснение под вопросом (одним сообщением, до 4096 символов).
* Режим **end**: «🧠 Решение» отвечает «Решения будут показаны в конце серии». На финише — один блок со всеми объяснениями по порядку вопросов.

## 1.8. Итоги серии

* Краткая статистика: «правильных/из N», время (опционально), тема.
* Кнопки: «Ещё по теме», «Играть ещё», «Другая тема», «🏠 Меню».
* Сброс активной сессии.

---

# 2) Принципы UX и текстовые гайды

* **Минимизация трения:** никаких этапов «регистрации». Первый экран — уже полезный.
* **Плоский и предсказуемый UI:** все действия на 1–2 тапа, без скрытых состояний.
* **Короткие сообщения:** 1–7 строк основного текста; таблицы — только в моноформате.
* **Ясные статусы:** после принятия ответа всегда появляется «Дальше →».
* **Идемпотентность:** повторный тап по той же кнопке не должен ломать состояние.
* **Тон общения:** дружелюбный, на «вы» нейтрально-деловой, без жаргона. Подсказки краткие.
* **Локализация:** RU по умолчанию, строки UI вынесены в словарь (позже).

---

# 3) Состояния и управление ими (FSM-уровень)

**Состояния:**

* `idle` — нет активной серии;
* `asking` — показываем вопрос, ответ ещё не принят;
* `answered` — ответ принят, доступно «Дальше →»;
* `await_text_input` — ждём текст/число от пользователя.

**Переходы (ключевые):**

* `idle → asking` (старт серии/темы/дня);
* `asking → answered` (single/multi commit/валидный текст/число);
* `answered → asking` (следующий вопрос) либо `answered → idle` (финиш);
* `asking ↔ await_text_input` (ввод/отмена ввода);
* `* → idle` (Домой/завершить серию с подтверждением — по политике).

**Идемпотентность:** обработчики должны безопасно выполнять повторные коллбеки; UI после каждого шага — актуальный.

---

# 4) Обработка данных и ограничения платформы

* **Telegram HTML:** без LaTeX и сложной разметки; для таблиц — моноширинный блок.
* **4096 символов** лимит на сообщение — следим за длиной `statement`/объяснений.
* **64 байта** лимит на `callback_data` — в коллбеках передаём только короткие токены (индексы, действие), не текст вариантов.
* **Дедуп коллбеков**: защита от двойных тапов обязательна (реализовано щитом).
* **Текстовый ввод** существует только как обычные сообщения пользователя:

  * «Ввожу» отражается флагом в сессии; все прочие тексты игнорируются, если не ожидаем ввода.

---

# 5) Работа с контентом (банк вопросов)

* Источник истины: `topics.json`, `questions.json`. Агент **не** изобретает вопросы/ответы/объяснения.
* Отбор вопросов: по `topic_ids` и `difficulty`, строго N = `qcount`. Если меньше — сценарий «мало вопросов».
* Порядок вопросов серии случайный; в итогах — по порядку серии.
* Варианты для choice-типов — короткие (в идеале ≤ 40–50 символов).
* Если нужно показать большую таблицу или схему — разрешено приложить как отдельный файл, а в тексте — краткий заголовок и указание «см. вложение» (на будущее).

---

# 6) Пользовательские данные и персистентность

* **Создание пользователя**: при первом `/start` записать профиль с дефолтными `prefs`, `session=null`.
* **Сессия**: хранить `mode`, `topic_id`, `question_ids`, `index`, `answers`, `await_input`, `solutions_qids`.
* **Ответы**: в `answers[qid]` хранить нормализованное значение, формат и флаг корректности.
* **Статистика**: счётчики total и по темам обновляются на каждом принятом ответе.
* **Запись на диск**: при любом существенном изменении (`prefs`, `answers`, `index`, `finish`), атомарно.

---

# 7) Обработка ошибок и крайних случаев

* **Мало вопросов в теме:** показать одноразовый алерт и остаться на экране тем. CTA: «Другая тема», «Снизить сложность».
* **Повторные клики:** отвечать «Уже обрабатываю…» без побочных эффектов.
* **Ввод невалидного числа/текста:** мягкая подсказка, остаться в режиме ввода. Предлагать «Отмена ввода».
* **Тайм-аут ожидания ввода:** через разумный период (например, 2–5 минут) сбрасывать флаг ожидания и сообщать об этом; вопрос остаётся в состоянии `asking`.
* **Потеря сессии (рестарт бота):** при наличии `session` предложить «⏸ Продолжить» в «Играть».
* **Конфликты состояния (редкие):** при несоответствии индексов/списков безопасно возвращать в `asking` или `idle` с пояснением «Обновил состояние, можно продолжать».

---

# 8) Политики проверки ответов

* **numeric**: удалить пробелы, `,` заменить на `.`, допуск ±0 только для целых (без погрешностей). Сравнение как строки запрещено для чисел.
* **text**: `lower` + trim + схлопнуть пробелы; пунктуацию по краям удалить; сравнить строку 1-к-1. Синонимы/регексы — **за рамками** MVP.
* **single_choice**: сравнение по индексу правильного варианта.
* **multi_choice**: сравнение множеств индексов, порядок не важен.
* **Подсказки/решения**: брать из `solution_explanation` вопроса; если поле пустое — коротко «Решение недоступно для этого вопроса».

---

# 9) Нефункциональные требования

* **Производительность:** ответ на коллбек ≤ 500 мс при прогретом процессе; загрузка банка — при старте.
* **Надёжность:** записи `users.json` атомарные; хранить последнюю резервную копию.
* **Безопасность:** никакой персональной чувствительной информации; никакой сторонней телеметрии; всё локально.
* **Логи:** события высокого уровня: старт серии, принятие ответа (с корректностью), завершение серии, ошибки ввода, ошибки банка (мало вопросов).
* **Миграции:** версии схем в корне файлов (`version`) и верхний `updated_at` для `users.json`.

---

# 10) Интеграционные контрактные точки (без кода)

* **Темы:** брать список из `topics.json`. Никакой «тестовой» константы в коде.
* **Подбор серии:** `topic_id` (или None), `diff`, `qcount`. Если ответ пустой — сценарий «мало вопросов».
* **Текущий вопрос:** определяется по `session.question_ids[session.index]`.
* **Принятие ответа:** по типам; после успеха — активировать «Дальше →».
* **Переход дальше:** индекс ++; если вышли за предел — «Итоги серии» и сброс `session`.
* **Решения:** немедленно показывать текст (imm) или копить `qid` и отдать сводно (end).

---

# 11) Тексты интерфейса (референс)

*(Короткие эталоны для копирайта; хранятся как константы строк, здесь — как образцы.)*

* Приветствие: «Привет! Это демо-квиз-бот. Выберите действие:»
* Мало вопросов: «В этой теме пока мало вопросов. Выберите другую тему или снизьте сложность.»
* Ожидание числа: «Жду число. Отправьте одним сообщением. Запятая = точка.»
* Ожидание текста: «Жду ответ текстом. Отправьте одним сообщением.»
* Невалидный ввод: «Не получилось распознать ответ. Проверьте формат и попробуйте ещё раз.»
* Решение «в конце»: «Решения будут показаны в конце серии.»
* Итоги: «Итоги серии: 3 из 5 верно.»

---

# 12) Критерии приёмки (Acceptance)

1. `/start` создаёт пользователя (если новый) и показывает меню.
2. «Темы» строятся из `topics.json`; пагинация работает.
3. Старт серии по теме: при достаточном банке — первый вопрос; при недостаточном — корректный алерт без падения.
4. Все 4 типа заданий корректно принимают ответы согласно нормализации; после принятия появляется «Дальше →».
5. Режимы решений: `imm` — тут же; `end` — сводно на финише.
6. «⏸ Продолжить» поднимает текущий вопрос после рестарта процесса.
7. Никаких «битых» состояний при двойных тапах, отменах ввода и возврате в меню.
8. `users.json` обновляется при каждом ответе/изменении настроек/финише и остаётся валидным JSON.

---

# 13) Не-цели (вне рамок этого релиза)

* Генерация новых заданий/вариантов ИИ-моделью.
* Семантические синонимы, толеранс к ошибкам в текстовых ответах.
* Групповые чаты и совместное прохождение.
* Личный рейтинг/глобальная таблица лидеров.
* Многоязычность и сложные формулы (LaTeX).

---

# 14) Дальнейшие расширения (дорожная карта)

* «Мягкий онбординг» (2 шага: сложность + длина серии) перед первым стартом.
* Синонимы/варианты корректных текстовых ответов на уровне `questions.json`.
* Индивидуальные подсказки перед ответом (не раскрывая решение).
* «Дневная серия» со streak-метрикой.
* Экспорт результатов/прогресса в файл.
