[project]
name = "tg-quiz-bot"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
    "telebot>=0.0.5",
]

[tool.uv]
package = true

[project.scripts]
bot = "app.__main__:main"
